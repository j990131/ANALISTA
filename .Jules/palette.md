## 2024-05-15 - ARIA attributes for dynamic carousels
**Learning:** Interactive accessibility in dual implementations (vanilla HTML vs React components) requires manual state syncing for standard attributes like aria-selected in vanilla JS, whereas React handles it declaratively.
**Action:** Always verify state attributes are manually updated in JS when migrating or duplicating React components into standalone vanilla HTML previews.
