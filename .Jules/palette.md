## 2026-03-13 - [Adding Spanish ARIA labels and roles]
**Learning:** Adding ARIA roles and labels is essential for Spanish apps. `role="tablist"` and `aria-live="polite"` for dynamic content were needed.
**Action:** Ensure dynamic JS implementations also explicitly manage `aria-selected` and initialize roles via attributes.
