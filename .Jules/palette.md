## 2024-07-22 - Dynamic Accessibility in DentalPromo
**Learning:** Dynamic UI elements like carousels need manual syncing of ARIA states in vanilla HTML compared to React.
**Action:** Always implement robust DOM node attribute updates (e.g., `aria-selected`) when porting React components to vanilla JS.
