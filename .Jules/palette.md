## 2024-07-14 - Add aria-labels to icon-only buttons
**Learning:** Dynamic UI components, such as carousels, require explicitly translated ARIA labels (e.g., 'Producto anterior', 'Producto siguiente') for accessibility, especially when the UI is localized (e.g., Spanish).
**Action:** Always add ARIA labels to icon-only buttons, translating them to match the UI language.
