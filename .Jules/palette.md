## 2026-06-25 - [DentalPromo Accessibility]
**Learning:** In multi-language UI carousels, ARIA labels must be explicitly translated (e.g., 'Producto anterior') and dynamically interpolated for mapped elements to ensure accessibility.
**Action:** Always verify that vanilla HTML implementations sync states like `aria-selected` via JavaScript to maintain parity with React's declarative behavior.
