## 2024-03-24 - Dynamic ARIA Attributes for Carousels
**Learning:** Implementing manual state attributes (like aria-selected) and dynamically interpolating labels is critical for maintaining accessibility parity between standard React declarative components and vanilla HTML implementations.
**Action:** Always ensure vanilla HTML dual implementations of React components manually sync necessary state attributes via JavaScript.
