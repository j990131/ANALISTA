## 2026-07-19 - Component ARIA Parity
**Learning:** When creating standalone HTML previews of React components with dynamic state (like carousels), ARIA attributes like `aria-selected` and keyboard interactions must be manually synchronized via JS to maintain accessibility parity.
**Action:** Always replicate interactive state management explicitly in vanilla HTML versions of components.
