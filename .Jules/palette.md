## 2024-08-11 - Carousel Accessibility
**Learning:** Custom carousels need specific ARIA roles to be accessible. Use `role="region"` and `aria-roledescription="carousel"` for the stage, and `role="group"` and `aria-roledescription="slide"` for individual cards.
**Action:** Always apply these roles to custom carousel implementations.
