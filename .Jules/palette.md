## 2024-08-05 - Spanish UI ARIA Labels
**Learning:** When adding ARIA labels to a UI with non-English content, such as this Spanish UI, the ARIA labels must also be translated to match the UI language (e.g., 'Producto anterior' instead of 'Previous product').
**Action:** Always check the language of the UI and ensure ARIA labels are translated accordingly.
