## 2024-10-24 - Add ARIA Labels to Interactive Elements
**Learning:** Dynamic UI components like carousels need explicit ARIA states (`aria-selected`, `role='tablist'`) and live regions (`aria-live='polite'`) to ensure proper screen reader accessibility.
**Action:** Ensure custom carousels and paginations have the proper `role` attributes and aria descriptions.
