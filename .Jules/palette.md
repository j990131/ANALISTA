## 2024-08-15 - Dynamic Carousel Accessibility
**Learning:** The custom carousel lacked screen reader support, requiring tab roles on dots and aria-live on the dynamic text container to announce product changes.
**Action:** Always add ARIA states and roles when implementing custom interactive UI elements that replace native browser behavior.
